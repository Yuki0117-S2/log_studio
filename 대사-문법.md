# 대사 입력 문법

기존 문법을 유지하면서 네 로그 편집기에 다음 입력을 추가했습니다.

```text
<<리바이>> "뭐라고 했냐."
<<레이나>> "거짓말."
>>"캐릭터 대사"
<<"유저 대사"
```

- **조각로그:** `<<이름>> "대사"`를 기존 `[이름]"대사"`처럼 처리합니다. **테마 → 현재 테마**에서 같은 인물의 색을 함께 변경합니다. `>>`는 캐릭터, `<<`는 유저입니다.
- **Log Cupcake:** 이름이 기존 AI·USER 이름과 같으면 해당 화자를 사용합니다. 다른 이름은 **추가 캐릭터**에 자동 등록되어 🎨 버튼으로 색을 변경합니다. 기존 추가 캐릭터 한도인 10명을 넘으면 자동 인물색으로 표시합니다. `>>`는 AI, `<<`는 USER이며 `AI: >통신` 등 기존 문법은 유지합니다.
- **Log Diary:** 같은 이름은 같은 자동 색으로 표시합니다. `>>`는 ‘캐릭터’, `<<`는 ‘유저’로 표시합니다.
- **Memorial Log:** **문장 입력칸**에 한 사람의 대사를 입력하고 칸을 벗어나면 문장과 화자를 나눠 담습니다. `>>`는 설정된 BOT 이름, `<<`는 설정된 USER 이름을 사용합니다. 한 번의 실행 취소로 되돌릴 수 있습니다.

이름은 1~24자이며 `<<이름>>` 뒤의 닫힌 큰따옴표 대사를 인식합니다. 스마트 따옴표와 `「대사」`, `『대사』`도 지원합니다. 이름 없는 `>>"대사"`, `<<"대사"`는 컵케이크·다이어리·메모리얼에서 한 줄 전체를 대사로 쓸 때 적용합니다. 조각로그의 기존 문장 속 화자 표시는 유지합니다.

조각·컵케이크·다이어리는 입력 원문을 유지하며 미리보기와 출력에 새 문법을 적용합니다. 기존 `[이름]`, `AI:`, `USER:`, `CHAR-01:`, `**이름** "대사"` 문법은 각 편집기에서 계속 사용할 수 있습니다. Arca Studio는 완성된 HTML을 편집하는 도구이므로 입력 문법을 추가하지 않았습니다.

## 검증

```sh
node --test tests/speaker-syntax.test.cjs
```

공통 파서의 충돌·미완성 입력·코드와 링크 보호·HTML 이스케이프를 검사합니다. 브라우저에서도 조각로그의 대사 스타일 5종과 인물별 색 변경, 컵케이크 미리보기와 게시용 HTML, 다이어리 렌더링, 메모리얼 두 진입 화면의 화자 분리 및 되돌리기를 확인했습니다.
